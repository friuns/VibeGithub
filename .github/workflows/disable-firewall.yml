name: Disable Copilot Firewall

on:
  workflow_dispatch:

jobs:
  disable-firewall:
    runs-on: ubuntu-latest
    steps:
      - name: Instructions
        run: |
          echo "This workflow demonstrates how to disable the Copilot firewall."
          echo ""
          echo "Due to GitHub token permissions, this workflow cannot directly modify"
          echo "repository variables. To disable the firewall manually, run:"
          echo ""
          echo "gh api repos/YOUR_REPO/actions/variables/COPILOT_AGENT_FIREWALL_ENABLED -X PATCH -f value=false"
          echo ""
          echo "Replace YOUR_REPO with your repository name (e.g., friuns/VibeGithub)"
          echo ""
          echo "Current status check:"
          echo "Note: This will fail due to permissions, but shows the command format"
